Copy the title.principals.tsv file into this folder

1. Run the split.py scipt to split the files into multiple chunks 
2. Run the title.principals.sql in your mySQL workbench

it's gonna take some time. 